const express = require('express');
const router = express.Router();

// Placeholder for services routes
router.get('/', (req, res) => {
  res.json({ message: 'Services routes - coming soon' });
});

module.exports = router;
